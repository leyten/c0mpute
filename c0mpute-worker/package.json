{
  "name": "@compute-network/worker",
  "version": "2.9.0",
  "description": "Native CLI worker for Compute Network (compute.tech), the distributed inference network. Runs LLM inference via ollama and connects to the orchestrator via Socket.io.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "compute-worker": "dist/index.js",
    "c0mpute-worker": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "keywords": [
    "llm",
    "inference",
    "distributed",
    "gpu",
    "ollama",
    "worker",
    "compute-network"
  ],
  "dependencies": {
    "commander": "^12.1.0",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
